# Dev: relative path — Vite proxies /api to the backend (see vite.config.ts).
# Deployed frontend: set the absolute backend URL, e.g. https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1

# Where the Vite dev server forwards /api (dev only)
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
